<div id="interno2">
<% title "FAPEA", :id =>"FAPEA" %>
 <div class="tabs">
   <div id="caixa_titulo">
    <font size="3"><strong>EDITAR CONTEÚDO LANÇADO</strong></font><br/>
   </div>
  <div class="tabs">
    <% form_remote_tag :url => editar_conteudo_path, :method => :get,:loading => "$('#status').show()" , :success => "$('#status').hide()" do %>
      <table width="100%" align="center" bgcolor="#DCDCDC" >
       <tr bgcolor="#F5F5F5">
             <td colspan="4"><font color="red" size="3"><b><u> ATENÇÃO:</u> Este sistema de controle das atividades desenvolvidas pelos profissionais da educação.</td>
        </tr>

        <%if current_user.has_role?('professor_infantil') or current_user.has_role?('professor_fundamental') %>
         <tr bgcolor="#F5F5F5">
           <td colspan="2"><font color="red"size="3"><b> &nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp &nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp Professores só podem editar e alterar relatórios até as 23:59 do dia que foi feito, apartir do dia seguinte somente gestor ou pedagogo podem alterar relatório gravado. <br/> Nesta tela aparecem somente os relatórios que podem ser editados para visualizar todos os relatórios existentes utilize a opção CONSULTAR.

               </b></font><hr/><br/>
           </td>
         </tr>

         <%end%>
          <tr>
            <td>
              <!--<a href="#" class="filtro"><%= radio_button_tag(:type_of, "1") %></a>Data-->
              <a href="#" class="filtro_fapeac"><%= radio_button_tag(:type_of, "4") %></a>Classe <b><font color =" blue">*</font></b>

              <%  if current_user.has_role?('admin') or   current_user.has_role?('pedagogo')	  or   current_user.has_role?('direcao_infantil')  or   current_user.has_role?('SEDUC')  or   current_user.has_role?('direcao_fundamental')  or   current_user.has_role?('supervisao') or current_user.has_role?('secretaria_infantil') or current_user.has_role?('secretaria_fundamental')%>
                  <a href="#" class="filtro_unidade"><%= radio_button_tag(:type_of, "3") %></a>Professor <b><font color =" blue">*</font></b>
                     <%  if current_user.has_role?('admin') or   current_user.has_role?('pedagogo')	 or   current_user.has_role?('SEDUC') or   current_user.has_role?('supervisao')%>
                               <a href="#" class="filtro_fapea"><%= radio_button_tag(:type_of, "2") %></a>Unidade<font color =" blue">*</font>
                     <%end%>
              <%end%>
              &nbsp&nbsp&nbsp&nbsp(<font color =" blue"><b>* Ano Letivo atual</b></font>)
            </td>
          </tr>

         <tr>
             <td colspan="2"><div class="consulta" style="display:none"> <%= render "consulta_data" %> </div>
             <div class="consulta_classe_fapea" style="display:none"> <%= render "consulta_classe" %> </div>
             <div class="consulta_fapea" style="display:none"> <%= render "consulta_unidade" %> </div>
             <div class="consulta_nome_fapea" style="display:none"> <%= render "consulta_professor" %> </div>

           </td>
        </tr>

        </table>
    <% end %>
    <br/>
    <hr/>
    <div id="relatorio"><div id='status' style='display:none;' align="center" ><%= image_tag("/stylesheets/images/loading.gif") %></div></div>
    <br/>

  <table>
    <tr>
     <td width="6%"align="left" ><%= button_to 'Fechar', home_path, {:method => "get"}  %></td>
     <td align="left"><%= button_to 'Nova Consulta', editar_conteudos_path, :title => "Nova Consulta" %></td>
    </tr>
  </table>
 </div>
 </div>
  <br/>
</div>



