

    <div id="interno2">
<% title "ALUNOS".singularize %>
<div class="tabs">
 <div id="caixa_titulo">
     <font size="3"><strong>AGENDA DE SUBSTITUIÇÕES - POR UNIDADE</strong></font><br/>
 </div>
    <table  width="100%"  bgcolor="#DCDCDC">
      <tr >
          <%if @reservar_salas.present? %>
          <td>
              
          </td>
          <%end%>
          <td colspan="2" width="95%" align="right ">

               <% form_for @search do |f| %>

                 <font size="3"> Unidade:</font>
                 <%= f.collection_select :unidade_id_equals, @unidades_infantil, :id, :nome, :include_blank => true%>
                 <%= f.submit "Visualizar", :class => "btn_filtrar" %>
              <% end %>


         </td>
       </tr>
       <tr bgcolor="#F5F5F5">
           <td colspan="3" align="right">
                <%= render :partial => "calendar" %>
               <%t=0%>
                <%if !@aulas_eventual.present? %>
               <%t=0%>
                     <%if (params[:search]).present? %>
                          <%= button_to'Cadastrar Aula Eventual',:action => "new", :unidade_id => params[:search][:unidade_id_equals] %>
                      <%end%>
                 <%end%>

          </td>
       </tr>

              <tr>
                <td align="right" ><%= button_to 'Fechar', home_path, {:method => "get"}  %></td>
                
                <td width="2%"align="left" ><%= button_to 'Imprimir', onclick="javascript:MM_openBrWindow('#{impressao_unidade_path}','impressao', 'scrollbars=yes');", :title => "Imprimir" %> </b></td>
                <%if (params[:search]).present? %>
                    <% if current_user.has_role?('SEDUC') or current_user.has_role?('admin')%>
                       <td width="2%" align="right"><%= button_to'Cadastrar Substituição',:action => "new", :unidade_id => params[:search][:unidade_id_equals],:target => '_blank' %></td>
                    <%end%>
                <%end%>

               </tr>
            </table>
</div>
<br/>
</div>