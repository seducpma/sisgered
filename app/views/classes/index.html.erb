<h1>Listing classes</h1>

<table>
  <tr>
    <th>Classe</th>
    <th>Responsável</th>
    <th>Unidade</th>
    <th>Auxiliar</th>
    <th>Estagiario</th>
    <th>Descricao</th>
  </tr>

<% @classes.each do |class| %>
  <tr>
    <td><%=h class.classe %></td>
    <td><%=h class.professor_id %></td>
    <td><%=h class.unidade_id %></td>
    <td><%=h class.auxiliar %></td>
    <td><%=h class.estagiario %></td>
    <td><%=h class.descricao %></td>
    <td><%= link_to 'Show', class %></td>
    <td><%= link_to 'Edit', edit_class_path(class) %></td>
    <td><%= link_to 'Destroy', class, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New class', new_class_path %>