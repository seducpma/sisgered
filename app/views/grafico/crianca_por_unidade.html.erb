
<div id="interno2">
<% title "GRAFICOS".singularize %>
<div class="tabs">
 <div id="caixa_titulo">
     <font size="3"><strong>GRÁFICO DEMANDA POR UNIDADE</strong></font><br/>
 </div>
  <%= form_tag({ :method => 'post', :action => 'search' }, :id => 'graph-unit-form')%>
    <%= error_messages_for 'contact' %>
 <% if $uni==1 %>
  <table>
    <div class="botao">
      <tr>
        <th width="30%">
          <div class="botao">Selecione a Unidade desejada.</div>
        </th>
        <td width="20%" align="center">
          <div class="botao"><%= collection_select :contact, :grafico_id, @unidades, :id, :nome%></div>
        </td>
        <td><div class="botao"><%= submit_tag 'Filtrar!' %></div></td>
      </tr>
    </div>
    </table>
 <%end%>
  <br/>
<table width="100%">
  <tr>
      <td align="center"><div class="botao"><%= @graph %></div></td><br/>
      <% if @static_graph.present?%>
        <td><div class="tela"><%= @static_graph%></div></td>
      <%end%>
  </tr>
</table>
<table>
   <% if $menu==1 %>
    <tr>
      <div class="botao">
         <td width="40%"></td>
        <td width="50%" align="left">
           |<%= link_to("Voltar", grafico_path) %>|</td>
        <td width="10%" align="right">
           <input type="Button" Value="Imprimir" onclick="Imprimir()"> </td>

    </div>
  </tr>
  <%end%>
</table>




</div>
<br/>
</div>


