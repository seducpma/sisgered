<% form_for (@transferencia) do |f| %>
 <%= f.error_messages %>
  <font  size="2">
    <br/>
    
    <table width="100%" align="center" bgcolor="#F5F5F5">
        <tr> <td></td> </tr>
        <tr>
          <td><b>Procedência: </b></td><td><%= f.collection_select :de, @unidade_de1, :nome, :nome, :include_blank => ' -- Selecione -- ' %>
                     <%= observe_field 'transferencia_de',
                          :with => :transferencia_de,
                          :url => { :action => 'unidade_transferencia' },
                          :update => "alunos_trans" %>
          </td>

        </tr>
        <tr>
          <td colspan="2">
            <div id="alunos_trans">
              <table>
                <tr>
                  <td width ="19%"><b>Aluno: </b>&nbsp&nbsp&nbsp<%= f.collection_select :aluno_id, @alunos, :id, :aluno_nome, :include_blank => ' -- Selecione -- ' %> </td>
                </tr>
                <tr>
                  <td width ="19%"><b>Classe: </b>&nbsp&nbsp&nbsp<%= f.collection_select :classe_id, @classes, :id, :classe_classe, :include_blank => ' -- Selecione -- ' %></td>
                </tr>
              </table>
           </div>
          </td>
        </tr>
        <tr>
          <td colspan="2">
        <b>Data Transferência: </b><%= f.date_select(:data_transferencia, :order => [:day, :month, :year], :start_year => (Date.today.year), :end_year => (Date.today.year)-1)%>    
          </td>
        </tr>

        <tr> <td></td> </tr>
      </table>
       <table width="100%" >
         <tr> <td> <br/></td> </tr>
         <tr>
            <td width="4%"align="left"><%= f.submit 'Salvar' %></td>
<%end%>
            <td><%= button_to 'Fechar', home_path, {:method => "get"}  %></td>
         </tr>
       </table>
      
  </font>